# Koha session cookie (CGISESSID) — get this from your Koha instance
# For the Next.js catalog app, copy this to catalog/.env.local
# For the test scripts (server.js, koha_test.js), run: source .env

export KOHA_SESSION_COOKIE=your_session_cookie_here
